define <%= @object_name %> {
  register 0
  name custom-vars-<%= @title %>
  <% @variables.keys().sort().each do |k| -%>
  <%- next if @variables[k].nil? or @variables[k] == :undef -%>
  <%= k %> <%= @variables[k] %>
  <% end -%>
  <% if @use -%>
  use <%= @use %>
  <% end -%>
}
